#include <stdio.h>
#include <string.h>
#include "mathey.h"
int calc() {
/* This don't work.
	char equ[80];
	char *p;
	int i = 0;
	int a[10];
	scanf("%[^\n]", equ);
	printf("%i", i);
	while (p = strchr(equ,'+')) {
		i = (int) p - (int) &equ;
		sscanf(equ, "%i", a[0]);
		for();		
	}
	printf("%i", a[0]);
*/
	return 0;
}
